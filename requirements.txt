# --- Core data ---
pandas>=2.2
numpy>=1.26
pyarrow>=15.0
appdirs>=1.4

# NOTE: nfl_data_py is installed separately via --no-deps in the setup steps below.
# Its setup.py pins pandas<2.0, but it works fine with modern pandas/numpy in practice.
# Installing it normally here would create an unsolvable dependency conflict with
# the other packages in this file (e.g. seaborn requires pandas>=1.2, which is fine,
# but pip's resolver still fails trying to satisfy nfl_data_py's stale upper bound).

# --- Database ---
sqlalchemy>=2.0
psycopg2-binary>=2.9
supabase>=2.4

# --- Machine learning ---
scikit-learn>=1.4
xgboost>=2.0
lightgbm>=4.3
scipy>=1.12
joblib>=1.4

# NOTE (macOS only): LightGBM requires OpenMP, which isn't bundled and often
# isn't installed by default on Mac. If you hit an error like
# "Library not loaded: @rpath/libomp.dylib", run: brew install libomp

# --- API ---
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
python-dotenv>=1.0

# --- HTTP / data collection ---
requests>=2.31
httpx>=0.27

# --- Notebooks / EDA ---
jupyter>=1.0
matplotlib>=3.8
seaborn>=0.13

# --- Testing ---
pytest>=8.0
pytest-cov>=5.0

# --- Dev tooling ---
black>=24.0
ruff>=0.4